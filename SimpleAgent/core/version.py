AGENT_VERSION = 'v0.8.2'

# Updates Under Here
# Added a verbose mode to the terminal to show the users more detail on what the agent is doing,
# Fixed the Output Files section to properly show file names and to allow for file downloading and viewing in a IDE style popup,
# Made the tool usage messages be buttons to show what the agent did,
# Added a way to delete files that the agent makes from the Output Files list,
# Fixed up the 404 and 500 error pages + clean up routes,
# Added firebase for user auth and managment,
# Added freemium - let users test the agent 3 times before being asked to sign up,
# Rebuilt the discord bot to use the websocket server
